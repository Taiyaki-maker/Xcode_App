/*
import SwiftUI

struct InformationView: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 20) {
            Text("アラームアプリについて")
                .font(.title)
                .padding(.bottom, 10)
            Text("このアプリは、バーコードをスキャンすることでアラームを停止することができます。")
            Text("アラームが鳴ると、指定したバーコードをスキャンするまでアラームが止まりません。")
            Text("バーコードを設定し、アラームが鳴ったときにそのバーコードをスキャンしてください。")
            Text("起床をサポートするためのアプリです。")
            Spacer()
        }
        .padding()
    }
}

struct InformationView_Previews: PreviewProvider {
    static var previews: some View {
        InformationView()
    }
}*/

import SwiftUI

struct InformationView: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 20) {
            Text("アラームアプリについて")
                .font(.title)
                .padding(.bottom, 10)
            Text("このアプリは、バーコードをスキャンすることでアラームを停止することができます。")
            Text("アラームが鳴ると、指定したバーコードをスキャンするまでアラームが止まりません。")
            Text("バーコードを設定し、アラームが鳴ったときにそのバーコードをスキャンしてください。")
            Text("起床をサポートするためのアプリです。")
            Spacer()
        }
        .padding()
    }
}

struct InformationView_Previews: PreviewProvider {
    static var previews: some View {
        InformationView()
    }
}

